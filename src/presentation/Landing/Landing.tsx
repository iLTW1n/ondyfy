
const styles = {
    Container: {
        textAlign: 'center',
    },
};

const Landing = () => {
    return (
        <div style={styles.Container}>
        </div>
    );
}

export default Landing;
